{
  "name": "@kapar/shared-types",
  "version": "0.0.0",
  "private": true,
  "description": "Shared domain enums and types (kapar state machine, roles, sources) — the single source of truth for client and server.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit"
  }
}
